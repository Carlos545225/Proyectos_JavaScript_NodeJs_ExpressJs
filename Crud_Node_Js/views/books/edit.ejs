<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Editar Libro</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Spectral:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Spectral', serif; /* Fuente elegante con serifas */
            background: linear-gradient(135deg, #f0f2f0 0%, #e0e5e0 100%); /* Fondo con gradiente sutil */
            color: #333;
            line-height: 1.7;
        }
        .edit-book-container {
            margin-top: 60px;
            display: flex;
            justify-content: center;
        }
        .edit-book-card {
            border: 1px solid #ddd; /* Borde sutil */
            box-shadow: 0 6px 16px rgba(0, 0, 0, 0.1); /* Sombra suave y extendida */
            border-radius: 10px;
            overflow: hidden;
            width: 100%;
            max-width: 500px; /* Limitar el ancho del formulario */
            background-color: #fff;
            transition: box-shadow 0.3s ease-in-out;
        }
        .edit-book-card:hover {
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.18);
        }
        .edit-book-header {
            background-color: #7b68ee; /* Morado amatista */
            color: #fff;
            padding: 1.75rem 2rem;
            border-bottom: 1px solid #666;
            border-radius: 10px 10px 0 0;
            font-size: 2rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden; /* Para el efecto de brillo */
        }
        .edit-book-header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(transparent, rgba(255, 255, 255, 0.3), transparent);
            transform: rotate(-45deg);
            transition: all 0.4s ease-in-out;
        }
        .edit-book-header:hover::before {
            transform: translate(50%, 50%) rotate(-45deg);
        }
        .edit-book-body {
            padding: 2.5rem;
        }
        .form-group label {
            font-weight: 600;
            color: #555;
            margin-bottom: 0.75rem;
            display: block;
            font-size: 1.1rem;
        }
        .form-control {
            border: 1px solid #ced4da;
            border-radius: 0.35rem;
            padding: 0.9rem 1.1rem;
            margin-bottom: 1.75rem;
            font-size: 1.1rem;
            transition: border-color 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }
        .form-control:focus {
            border-color: #7b68ee; /* Morado amatista al enfocar */
            box-shadow: 0 0 0 0.2rem rgba(123, 104, 238, 0.25);
        }
        .update-button {
            background-color: #38c172; /* Verde esmeralda */
            color: #fff;
            border: none;
            padding: 1.1rem 2.2rem;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1.2rem;
            font-weight: 600;
            transition: background-color 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.12);
        }
        .update-button:hover {
            background-color: #2e9e5d;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }
        .alert {
            border-radius: 8px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            padding: 1rem 1.5rem;
            margin-bottom: 1.5rem;
            font-weight: 500;
        }
        .alert-danger {
            background-color: #f8d7da;
            border-color: #f5c6cb;
            color: #721c24;
        }
        .alert .close {
            font-size: 1.2rem;
            font-weight: bold;
            color: inherit;
            opacity: 0.7;
        }
    </style>
</head>
<body class="edit-book-container">

    <% if (messages.error) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-triangle mr-2"></i> ¡Oh no! <%- messages.error %>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    <% } %>

    <div class="edit-book-card">
        <div class="edit-book-header">
            <i class="fas fa-pen-fancy mr-2"></i> Editar Libro
        </div>
        <div class="edit-book-body">
            <form action="/books/update/<%= id %>" method="post">
                <div class="form-group">
                    <label for="name">Nombre del Libro:</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= name %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <label for="author">Autor del Libro:</label>
                    <input type="text" class="form-control" id="author" name="author" value="<%= author %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <button type="submit" class="btn update-button"><i class="fas fa-check-circle mr-2"></i> Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>